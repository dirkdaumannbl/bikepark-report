# Google Maps browser/embed key — referrer-restricted, public-by-design.
# Copy this file to `.env` for local dev, and set the SAME variable in
# Vercel → Settings → Environment Variables (Production + Preview).
# Leave empty to build without a live map (the map shows a graceful fallback note).
PUBLIC_GOOGLE_MAPS_KEY=
